# Rename this file's contents into your local .streamlit/secrets.toml
# (this file itself is a template — safe to commit).
# On Streamlit Community Cloud: App settings → Secrets → paste the same keys.
#
# NEVER commit your real secrets.toml with real values to git.
# Add ".streamlit/secrets.toml" to .gitignore if you fill in real values here.

[supabase]
url = "https://YOUR_PROJECT_REF.supabase.co"
key = "YOUR_SUPABASE_ANON_PUBLIC_KEY"

[app]
company_name = "ABC-MIB Group Co., Ltd."
